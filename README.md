# Nuvia Admin Panel

Админ-панель для интернет-магазина Nuvia на React + TypeScript + Vite.

## 🚀 Технологии

- **React 19** + **TypeScript**
- **Vite** - быстрая сборка
- **TailwindCSS** - стилизация
- **shadcn/ui** - UI компоненты
- **React Router v6** - маршрутизация
- **Axios** - HTTP клиент
- **Zustand** - управление состоянием
- **React Hook Form** + **Zod** - формы и валидация

## 📋 Требования

- Node.js >= 18.x
- npm или yarn
- Запущенный backend (nuvia-back)

## 🔧 Установка

1. **Установите зависимости:**

```bash
cd nuvia-admin
npm install
```

2. **Настройте переменные окружения:**

Создайте файл `.env` в корне проекта:

```env
VITE_API_URL=http://localhost:3000
```

3. **Запустите проект:**

```bash
npm run dev
```

Приложение будет доступно по адресу: http://localhost:5173

## 📁 Структура проекта

```
src/
├── api/              # API клиенты
│   ├── axios.ts      # Axios instance с interceptors
│   ├── auth.ts       # API авторизации
│   ├── categories.ts  # API категорий
│   ├── products.ts   # API товаров
│   └── uploads.ts    # API загрузки файлов
├── components/       # React компоненты
│   ├── ui/          # shadcn/ui компоненты
│   ├── ProtectedRoute.tsx
│   ├── Sidebar.tsx
│   ├── Navbar.tsx
│   └── UploadInput.tsx
├── pages/           # Страницы приложения
│   ├── Login.tsx
│   ├── Dashboard.tsx
│   ├── Categories/
│   │   ├── List.tsx
│   │   ├── Create.tsx
│   │   └── Edit.tsx
│   └── Products/
│       ├── List.tsx
│       ├── Create.tsx
│       └── Edit.tsx
├── store/           # Zustand stores
│   └── auth.ts
├── lib/             # Утилиты
│   └── utils.ts
├── App.tsx          # Главный компонент
└── main.tsx         # Точка входа
```

## 🔐 Авторизация

Для входа используйте учетные данные администратора:
- Email: `admin@nuvia.com` (или из seed скрипта)
- Password: `admin123` (или из seed скрипта)

JWT токен автоматически сохраняется в localStorage и добавляется к каждому запросу.

## 📚 Функции

### Категории
- Просмотр списка категорий
- Создание новой категории
- Редактирование категории
- Удаление категории

### Товары
- Просмотр списка товаров
- Создание нового товара
- Редактирование товара
- Удаление товара
- Загрузка основного изображения
- Загрузка нескольких дополнительных изображений
- Редактирование JSON фильтров

### Загрузка файлов
- Автоматическая загрузка на S3/R2
- Превью изображений
- Удаление загруженных изображений

## 🛠️ Команды

```bash
npm run dev      # Запуск в режиме разработки
npm run build    # Сборка для продакшна
npm run preview  # Предпросмотр продакшн сборки
npm run lint     # Проверка кода
```

## 🔒 Защита маршрутов

Все маршруты кроме `/login` защищены через `ProtectedRoute`. При отсутствии токена происходит редирект на страницу входа.

## 📝 API Endpoints

Админ-панель подключается к следующим эндпоинтам backend:

- `POST /auth/login` - Вход
- `GET /categories` - Список категорий
- `POST /categories` - Создание категории
- `PATCH /categories/:id` - Обновление категории
- `DELETE /categories/:id` - Удаление категории
- `GET /products` - Список товаров
- `GET /products/:id` - Получение товара
- `POST /products` - Создание товара
- `PATCH /products/:id` - Обновление товара
- `DELETE /products/:id` - Удаление товара
- `POST /upload` - Загрузка файла

## 🎨 UI Компоненты

Используются компоненты в стиле shadcn/ui:
- Button
- Input
- Textarea
- Select
- Card
- Table
- Dialog

Все компоненты стилизованы через TailwindCSS.

## 🐛 Решение проблем

### Ошибка подключения к API

Убедитесь, что:
- Backend запущен на порту 3000
- `VITE_API_URL` в `.env` правильный
- CORS настроен на backend

### Ошибка авторизации

- Проверьте, что токен сохраняется в localStorage
- Убедитесь, что backend возвращает правильный формат токена
- Проверьте, что JWT_SECRET на backend совпадает

### Ошибка загрузки файлов

- Проверьте настройки S3/R2 на backend
- Убедитесь, что bucket существует и доступен
- Проверьте права доступа к bucket

## 📄 Лицензия

MIT

---

**Приятной работы! 🚀**
